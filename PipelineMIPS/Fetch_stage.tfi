


// Instantiate the module
Fetch_stage instance_name (
    .clk(clk), 
    .branch_target(branch_target), 
    .pcSrc(pcSrc), 
    .hit(hit), 
    .pc_inc2(pc_inc2), 
    .instruction(instruction)
    );


